export declare const wait: (ms: number) => Promise<unknown>;
declare type Point = {
    x: number;
    y: number;
};
declare type DragOptions = {
    delta?: Point;
    to?: HTMLElement | Point;
};
export declare const SimulateDragAndDrop: (element: HTMLElement, { to: inTo, delta }: DragOptions) => void;
export {};
