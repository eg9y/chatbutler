import { BezierEdge } from 'reactflow';
import type { GetSmartEdgeOptions } from '../getSmartEdge';
import type { EdgeProps, Node } from 'reactflow';
export declare type EdgeElement = typeof BezierEdge;
export declare type SmartEdgeOptions = GetSmartEdgeOptions & {
    fallback?: EdgeElement;
};
export interface SmartEdgeProps<EdgeDataType = unknown, NodeDataType = unknown> extends EdgeProps<EdgeDataType> {
    nodes: Node<NodeDataType>[];
    options: SmartEdgeOptions;
}
export declare function SmartEdge<EdgeDataType = unknown, NodeDataType = unknown>({ nodes, options, ...edgeProps }: SmartEdgeProps<EdgeDataType, NodeDataType>): JSX.Element;
export declare type SmartEdgeFunction = typeof SmartEdge;
