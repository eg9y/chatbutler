const GptNodes = () => {
	return (
		<div className="divide-y divide-gray-200 overflow-hidden rounded-lg bg-white w-full">
			<div className="px-4 py-5 sm:p-6">
				GPT Nodes are nodes that make use of the GPT models that includes Chat API and
				Complete API.
			</div>
		</div>
	);
};

export default GptNodes;
