# Search Node

The search node enables you to upload a file and have it processed for making semantic search queries. 
The intensive file embedding process occurs only once, since future search requests for that file will 
reference the data processed initially.